{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^3.0.0"
  }
}
