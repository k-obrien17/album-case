---
phase: 02-this-or-that-ranking-mvp
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - web/src/main.ts
  - web/src/ui/pickLoop.ts
  - web/src/ui/rankedList.ts
  - web/src/storage.ts
  - web/src/session.ts
  - web/src/seed.ts
  - web/src/style.css
  - web/index.html
autonomous: false
requirements: [RANK-01, RANK-04, PLAY-01, PLAY-02, PLAY-03, PLAY-04]
user_setup: []

must_haves:
  truths:
    - "On first visit, with zero setup and no account, the player instantly sees two albums (cover, title, artist) and picks one"
    - "Each pick advances the loop to the next binary-insertion comparison the algorithm needs"
    - "The player can view their current ranked list at any time"
    - "The ranked list and the anonymous session survive a page refresh"
    - "The interface is usable on a phone: >=44px tap targets, no horizontal scroll at 360px"
    - "Album covers load live from the Cover Art Archive pointer without blocking the pick interaction"
  artifacts:
    - path: "web/src/ui/pickLoop.ts"
      provides: "Two-album pick UI wired to the ranking algorithm"
    - path: "web/src/ui/rankedList.ts"
      provides: "Ordered ranked-list view (RANK-04)"
    - path: "web/src/storage.ts"
      provides: "localStorage persistence of ranking state under tastetest-* keys"
    - path: "web/src/session.ts"
      provides: "Anonymous session id (UUID) creation + persistence"
  key_links:
    - from: "web/src/ui/pickLoop.ts"
      to: "web/src/ranking/insertion.ts"
      via: "nextComparison drives the shown pair, applyPick handles the pick"
      pattern: "nextComparison|applyPick"
    - from: "web/src/ui/pickLoop.ts"
      to: "web/src/storage.ts"
      via: "every pick persists RankingState before rendering the next pair"
      pattern: "save|persist"
    - from: "web/src/storage.ts"
      to: "localStorage"
      via: "tastetest-namespaced keys"
      pattern: "tastetest-"
---

<objective>
Build the player-facing surface: the two-album this-or-that pick loop, the ranked-list view, and localStorage persistence of the personal list plus an anonymous session. The covers are the visual centerpiece; the UI is phone-first and instant with zero setup.

Purpose: This is the first player-facing experience. It turns the algorithm (02-02) and the seed (02-01) into a playable loop that satisfies the RANK/PLAY requirements.
Output: A working, refresh-durable, phone-usable pick loop and list view under `web/src/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-this-or-that-ranking-mvp/02-CONTEXT.md
@te-tokens.css
@te-bridge.css

<interfaces>
From web/src/ranking/insertion.ts (plan 02-02 — use directly, do not reimplement):
  type RankingState = { ranked: Album[]; pending: { album: Album; lo: number; hi: number } | null }
  type Comparison = { candidate: Album; opponent: Album }
  startPlacement(state, album): RankingState
  nextComparison(state): Comparison | null
  applyPick(state, winnerMbid): RankingState

Album (seed record, web/public/seed/albums.json):
  { mbid, title, primary_artist_name, release_year, cover_url }

localStorage key namespace (per 02-CONTEXT.md — distinct from the legacy tool's `kob-calibration-v1`):
  tastetest-session   -> { session_id: UUID, created_at: ms }
  tastetest-ranking   -> serialized RankingState
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed loader, session, and localStorage persistence</name>
  <files>web/src/seed.ts, web/src/session.ts, web/src/storage.ts</files>
  <read_first>
    - web/src/ranking/insertion.ts, web/src/ranking/types.ts (RankingState shape to persist)
    - web/public/seed/albums.json (the album source to fetch)
    - .planning/phases/02-this-or-that-ranking-mvp/02-CONTEXT.md (localStorage namespacing + graceful-failure guidance)
  </read_first>
  <action>
    Write `web/src/seed.ts` to fetch `/seed/albums.json` at startup and expose the album pool plus a helper to pick the next unranked album (an album not yet in `ranked` and not the current pending candidate) as the next placement candidate. Write `web/src/session.ts` to read-or-create an anonymous session under `tastetest-session` using `crypto.randomUUID()` and an ms timestamp (`Date.now()`), persisting it. Write `web/src/storage.ts` with `saveRanking(state)` / `loadRanking()` against the `tastetest-ranking` key (JSON serialize/parse the RankingState). On any localStorage failure (private mode, quota), warn to console and continue in-memory without crashing (mirror the project's never-crash rule). Do NOT diverge in-memory state from what is written: save immediately after each mutation.
  </action>
  <acceptance_criteria>
    - `cd web && npx vitest run src/session` (or a storage test) confirms session creation returns a UUID and is idempotent across calls (same id on second read).
    - `grep -c "tastetest-" web/src/session.ts web/src/storage.ts` >= 2 (both namespaced keys present); `grep -c "kob-calibration" web/src/*.ts` == 0.
    - storage functions are wrapped so a thrown localStorage error is caught (grep: `grep -Ec 'catch' web/src/storage.ts` >= 1).
  </acceptance_criteria>
  <done>Seed loads, an anonymous session persists, and RankingState round-trips through localStorage without crashing on failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-album pick loop + ranked-list view, wired and phone-first</name>
  <files>web/src/ui/pickLoop.ts, web/src/ui/rankedList.ts, web/src/main.ts, web/src/style.css, web/index.html</files>
  <read_first>
    - web/src/ranking/insertion.ts (nextComparison / applyPick contract)
    - web/src/seed.ts, web/src/session.ts, web/src/storage.ts (task 1)
    - te-tokens.css, te-bridge.css (tokens: --color-accent #b9512a, --font-mono, square corners)
  </read_first>
  <behavior>
    - Renders two album cards (cover image, title, primary artist). Cover uses the pointer URL with async loading (loading="lazy" / no await) and a neutral placeholder box so a slow/missing cover never blocks the pick.
    - Clicking/tapping a card (or pressing 1 / 2, or ArrowLeft / ArrowRight) records the pick: calls applyPick, persists the new RankingState, then renders the next comparison from nextComparison; when a placement finalizes, startPlacement on the next unranked candidate.
    - A visible control toggles the ranked-list view, which shows the current `ranked` albums in order (index 0 first) with cover thumbnail + title + artist.
    - On load, the app restores RankingState from storage (or starts fresh) and the session, so a refresh mid-loop resumes on the same pair.
    - Tap targets (cards, buttons) are >=44px; layout has no horizontal scroll at a 360px viewport width.
  </behavior>
  <action>
    Write `web/src/ui/pickLoop.ts` rendering the two-card comparison and handling pointer + keyboard input, driving the loop through `nextComparison`/`applyPick` (from 02-02) and persisting via `storage.ts` after every pick. Write `web/src/ui/rankedList.ts` rendering the ordered list (RANK-04). Wire both into `web/src/main.ts`: create/restore session, load seed, restore RankingState, start or resume placement, and render.

    COLD-START BOOTSTRAP (mandatory — first-visit correctness). On an empty `ranked` list, per the 02-02 contract, `startPlacement` seats the first album immediately and `nextComparison` then returns `null` (nothing to compare against yet). So at startup, and again after any placement finalizes, run a loop: while `nextComparison(state)` is `null` AND unranked candidates remain, call `startPlacement` on the next unranked candidate and persist — repeat until `nextComparison` returns a real comparison, THEN render. This guarantees a true first-visit player (zero ranked albums) is shown a real two-album comparison, never a blank/crashed screen. Only if no unranked candidates remain (the whole seed is ranked) does the loop end without a comparison and the app shows the completed ranked list instead. Update `web/index.html`/`web/src/style.css` so covers are the visual centerpiece using te tokens only (no new palette, square corners, no entrance animations), tap targets >=44px, and the layout is fluid with no horizontal overflow at 360px. Build all DOM with safe construction (createElement/textContent), not innerHTML, consistent with the repo convention.
  </action>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0.
    - `web/src/ui/pickLoop.ts` references both `nextComparison` and `applyPick` (grep both present) and calls a storage save after a pick (grep: `save` or `persist`).
    - Covers load non-blocking: `grep -Ec 'loading=.lazy.|new Image|<img' web/src/ui/pickLoop.ts` >= 1 and there is no `await` on image load.
    - No `innerHTML` assignment in the ui modules (grep: `grep -c 'innerHTML' web/src/ui/*.ts` == 0).
    - style.css uses te tokens and no literal `#b9512a` (grep: `grep -c '#b9512a' web/src/style.css` == 0).
    - Cold-start: with localStorage cleared (zero ranked albums), the first render shows a real two-album comparison (not a blank/undefined/crashed screen). A unit or integration test seeds an empty state and asserts the startup bootstrap loop yields a non-null comparison before first render, and `main.ts` contains a `while`/loop that chains `startPlacement` while `nextComparison` is null.
  </acceptance_criteria>
  <done>The pick loop plays instantly, advances via the algorithm, persists every pick, exposes the ranked list, and resumes after refresh.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phone-usability + cover-load verification</name>
  <what-built>A running Vite dev server serving the two-album pick loop, the ranked-list toggle, and localStorage persistence, styled with the te design system.</what-built>
  <how-to-verify>
    1. `cd web && npm run dev`, open the printed URL. Confirm two albums appear immediately with cover, title, artist — no setup, no account.
    2. In DevTools device mode (or on a phone via LAN), set width to 360px: confirm NO horizontal scroll and that cards/buttons are comfortably tappable (>=44px).
    3. Make several picks, then open the ranked-list view: confirm the order reflects your picks and grows as you play.
    4. Refresh the page mid-loop: confirm the same pair (or resumed placement) and the ranked list persist.
    5. Throttle the network (Slow 3G): confirm you can still pick immediately while covers stream in behind a placeholder (pick never blocks on a cover).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any layout/persistence/cover-blocking issue.</resume-signal>
</task>

</tasks>

<verification>
- `cd web && npm run build` succeeds and `cd web && npx vitest run` passes.
- localStorage keys are `tastetest-*`; the legacy `kob-calibration-v1` key is never used.
- Human checkpoint confirms instant first-play, 360px no-scroll, >=44px targets, refresh-durability, and non-blocking covers.
</verification>

<success_criteria>
RANK-01, RANK-04, PLAY-01..04 are met: an anonymous, zero-setup, phone-usable pick loop presents two albums, advances via binary insertion, exposes the ranked list on demand, survives refresh, and streams covers without blocking picks.
</success_criteria>

<output>
Create `.planning/phases/02-this-or-that-ranking-mvp/02-03-SUMMARY.md` when done.
</output>
