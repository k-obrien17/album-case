---
phase: 02-this-or-that-ranking-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/vite.config.ts
  - web/tsconfig.json
  - web/vercel.json
  - web/.gitignore
  - web/index.html
  - web/src/main.ts
  - web/src/style.css
  - web/public/te-tokens.css
  - web/public/te-fonts.css
  - web/public/te-bridge.css
  - web/public/fonts/
  - web/public/seed/album-list.json
  - web/public/seed/albums.json
  - web/public/seed/README.md
  - web/api/_allowlist.json
  - build-seed.py
  - test_build_seed.py
autonomous: false
requirements: []
# Foundational scaffold + seed data. RANK-01 and PLAY-04 are delivered and
# verified in 02-03 (the pick loop + non-blocking cover render); this plan only
# produces the app shell and the album seed they depend on.
user_setup: []

must_haves:
  truths:
    - "The web/ project builds with vite and runs vitest"
    - "The Total Emphasis design system (IBM Plex Mono, terracotta, white bg, square corners) loads in the shell"
    - "A curated seed of >=200 well-known albums exists, each carrying mbid, title, primary artist, year, and a Cover Art Archive cover URL"
    - "The seed is explicitly marked a temporary bootstrap, not the permanent catalog"
  artifacts:
    - path: "web/public/seed/albums.json"
      provides: "The loop's album source (mbid, title, primary_artist_name, release_year, cover_url)"
      min_lines: 200
    - path: "build-seed.py"
      provides: "MusicBrainz-resolver build script that emits the seed"
    - path: "web/package.json"
      provides: "Vite + TypeScript + vitest project manifest"
    - path: "web/api/_allowlist.json"
      provides: "Compact array of seed MBIDs for backend payload whitelisting"
  key_links:
    - from: "web/public/seed/albums.json"
      to: "coverartarchive.org"
      via: "cover_url string built from release-group MBID"
      pattern: "coverartarchive\\.org/release-group/.*/front-500"
    - from: "web/index.html"
      to: "te-tokens.css / te-fonts.css / te-bridge.css"
      via: "stylesheet links loaded in cascade order"
      pattern: "te-bridge\\.css"
---

<objective>
Stand up the greenfield Taste Test product: a lean Vite + TypeScript single-page app under `web/`, wired to the Total Emphasis design system, plus the curated seed album dataset that feeds the pick loop. The legacy vanilla-JS tool at the repo root is NOT a foundation and is left untouched.

Purpose: Everything in Phase 2 needs (a) a buildable TS/Vercel project and (b) a real album source. Phase 1's full universe is data-pending (empty store), so a curated static seed unblocks the loop now.
Output: A building `web/` scaffold with te design tokens, and `web/public/seed/albums.json` (>=200 albums) plus a co-located MBID allowlist for the backend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-this-or-that-ranking-mvp/02-CONTEXT.md
@DATA-SOURCES.md
@pipeline/schema.sql
@pipeline/covers.py

<interfaces>
Album entity fields (from pipeline/schema.sql `entities`, album rows):
  entity_type='album', mbid TEXT, title TEXT, primary_artist_name TEXT,
  primary_artist_mbid TEXT, release_year INTEGER, cover_url TEXT

Cover Art Archive pointer template (verbatim from pipeline/covers.py, reuse — do NOT invent a new one):
  https://coverartarchive.org/release-group/{mbid}/front-500

Seed album JSON record shape (the loop's source; mirrors the album entity row minus store bookkeeping):
  { "mbid": string(UUID), "title": string, "primary_artist_name": string,
    "release_year": number|null, "cover_url": string }

MusicBrainz release-group search API (CC0, no key, 1 req/sec):
  https://musicbrainz.org/ws/2/release-group?query=...&fmt=json
  Response releasegroups[].id is the release-group MBID used in the cover template.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify npm package legitimacy before install</name>
  <what-built>No RESEARCH.md Package Legitimacy Audit exists, and this phase introduces the project's first npm dependencies. Per the package legitimacy gate, all packages are treated [ASSUMED] until confirmed. This checkpoint confirms each package is the real, intended one before any install runs.</what-built>
  <how-to-verify>
    Confirm each package on npmjs.com is the legitimate, widely-used package (not a typosquat):
    1. vite            -> https://www.npmjs.com/package/vite
    2. typescript      -> https://www.npmjs.com/package/typescript
    3. vitest          -> https://www.npmjs.com/package/vitest
    4. @libsql/client  -> https://www.npmjs.com/package/@libsql/client   (Turso client, used by the backend plan)
    5. @vercel/node    -> https://www.npmjs.com/package/@vercel/node       (types for the serverless handler)
    Each should show high weekly download counts and a recognized publisher (vitejs, Microsoft, vitest-dev, tursodatabase, vercel).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name any package to swap/drop.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold the web/ Vite + TypeScript SPA and adopt the te design system</name>
  <files>web/package.json, web/vite.config.ts, web/tsconfig.json, web/vercel.json, web/.gitignore, web/index.html, web/src/main.ts, web/src/style.css, web/public/te-tokens.css, web/public/te-fonts.css, web/public/te-bridge.css, web/public/fonts/</files>
  <read_first>
    - te-tokens.css, te-fonts.css, te-bridge.css (repo root — the design system to copy)
    - index.html (repo root — legacy shell; note the stylesheet load ORDER: fonts, tokens, app, bridge-last)
    - .planning/phases/02-this-or-that-ranking-mvp/02-CONTEXT.md (design + stack decisions)
  </read_first>
  <action>
    Create a lean Vite + vanilla-TypeScript single-page app in `web/` using npm (Keith's Vercel default). Dependencies: `@libsql/client` (runtime, for the backend plan). Dev dependencies: `vite`, `typescript`, `vitest`, `@vercel/node`. Add npm scripts: `dev` (vite), `build` (vite build), `test` (vitest run).
    Copy the three design-system stylesheets (`te-tokens.css`, `te-fonts.css`, `te-bridge.css`) and the `fonts/` directory from the repo root into `web/public/` so they ship with the app. Write `web/index.html` as a minimal shell that links them in the SAME cascade order the legacy shell uses (fonts, tokens, style, bridge LAST so bridge wins), sets the mobile viewport meta, and mounts a single `<div id="app">`. Write `web/src/main.ts` as the entry that imports `./style.css` and renders a placeholder heading (real UI arrives in plan 02-03). `web/src/style.css` holds only app-shell layout that consumes the te tokens (var(--color-bg), var(--color-accent) #b9512a, var(--font-mono), square corners) — no new palette. Configure `web/vercel.json` so the Vite build output is served and the `web/api/` directory is treated as serverless functions. Add `web/.gitignore` for `node_modules` and `dist`. Do NOT modify any repo-root legacy file.
  </action>
  <acceptance_criteria>
    - `cd web && npm install && npm run build` exits 0 and emits `web/dist/`.
    - `web/public/te-bridge.css` exists and `web/index.html` links all three te stylesheets with te-bridge.css LAST (grep: `grep -n te-bridge web/index.html` returns a line after the te-tokens link).
    - `web/index.html` contains a `width=device-width` viewport meta and a `<div id="app">` mount point.
    - `web/src/style.css` references te tokens, not hardcoded hex duplicates (grep: `grep -c 'var(--color' web/src/style.css` >= 1; and the terracotta hex `#b9512a` does NOT appear literally: `grep -c '#b9512a' web/src/style.css` == 0).
    - No repo-root file changed (git status shows only additions under `web/`, `build-seed.py`, `test_build_seed.py`).
  </acceptance_criteria>
  <done>web/ builds and serves a te-styled shell; the design system loads with bridge winning the cascade.</done>
</task>

<task type="auto">
  <name>Task 3: Build the curated seed album dataset from MusicBrainz</name>
  <files>web/public/seed/album-list.json, build-seed.py, web/public/seed/albums.json, web/api/_allowlist.json, web/public/seed/README.md, test_build_seed.py</files>
  <read_first>
    - pipeline/covers.py (reuse `_COVER_URL_TEMPLATE` exactly)
    - pipeline/schema.sql (album entity fields the seed record mirrors)
    - DATA-SOURCES.md (MusicBrainz is CC0, 1 req/sec, no key; covers via Cover Art Archive pointer, never a copied file)
  </read_first>
  <action>
    Create `web/public/seed/album-list.json`: a curated list of >=250 well-known "artist / album" pairs spanning eras and genres (the human-readable input, no MBIDs yet). Write `build-seed.py` (stdlib only, matching the existing pipeline tooling style) that, for each entry, queries the MusicBrainz release-group search API (`fmt=json`, User-Agent set per MB etiquette, sleeping >=1s between requests to honor the 1 req/sec limit), takes the best primary-type Album release-group match, and records `{mbid, title, primary_artist_name, release_year, cover_url}` where `cover_url` is built with the Cover Art Archive template imported/copied from `pipeline/covers.py` (`https://coverartarchive.org/release-group/{mbid}/front-500`). Emit `web/public/seed/albums.json` (array, the frontend source) and `web/api/_allowlist.json` (array of just the resolved MBID strings, co-located with the api/ dir so the backend function bundles it for payload whitelisting). Skip and log any entry that fails to resolve; do not emit partial records. Write `web/public/seed/README.md` stating in the first line that this is a TEMPORARY BOOTSTRAP seed, to be replaced by a subset export of the real materialized universe once the ~7GB dump is loaded (per 02-CONTEXT.md), NOT the permanent catalog. Write `test_build_seed.py` (pytest) validating the emitted `albums.json`.
    Do NOT fetch or store any cover image bytes — cover_url is a pointer string only.
  </action>
  <acceptance_criteria>
    - `python3 build-seed.py` runs and writes `web/public/seed/albums.json` and `web/api/_allowlist.json`.
    - `python3 -m pytest test_build_seed.py -v` passes, asserting: albums.json is a JSON array with >=200 records; every record has non-empty `mbid` (UUID format), `title`, `primary_artist_name`, and a `cover_url` matching `^https://coverartarchive\.org/release-group/[0-9a-f-]{36}/front-500$`; `release_year` is an int or null; `_allowlist.json` is an array of MBID strings equal to the set of album mbids.
    - No image files are written under `web/public/seed/` (grep: the seed dir contains only `.json`/`.md`).
    - `web/public/seed/README.md` line 1 contains "TEMPORARY" and "bootstrap" (case-insensitive).
  </acceptance_criteria>
  <done>A validated seed of >=200 albums with CAA cover pointers exists, plus a backend MBID allowlist, marked as a temporary bootstrap.</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` succeeds; `web/dist/` is produced.
- `python3 -m pytest test_build_seed.py -v` passes.
- `web/public/seed/albums.json` has >=200 valid records; `web/api/_allowlist.json` mirrors their MBIDs.
- te design system loads in the shell; no repo-root legacy file was modified.
</verification>

<success_criteria>
The Taste Test product project builds under `web/`, adopts the Total Emphasis design system, and ships a validated >=200-album curated seed (with CAA cover pointers) plus a backend MBID allowlist — the substrate the pick loop, algorithm, and mailbox build on.
</success_criteria>

<output>
Create `.planning/phases/02-this-or-that-ranking-mvp/02-01-SUMMARY.md` when done.
</output>
